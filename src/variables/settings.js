import {ref} from 'vue';

export const variables = ref({
                               debug: true,
                               setup: false
                             });

export default variables;
